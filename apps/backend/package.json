{
   "name": "@repo/backend",
   "private": true,
   "version": "0.0.0",
   "type": "module",
   "engines": {
      "node": "25.9.0"
   },
   "scripts": {
      "typecheck": "tsc -p tsconfig.json --pretty --noEmit",
      "format": "prettier --config ../../.prettierrc.json --write \"src/**/*.{ts,tsx}\""
   }
}
